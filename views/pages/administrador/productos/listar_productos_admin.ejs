
<%- include('../../../partials/head'); %>

<body>
        <%- include('../../../partials/header'); %>
    <main>
        <div class="container">
            <h1>Productos</h1>
            <a href="#" class="btn btn-success mb-3">Agregar</a>
            <table class="table">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Descripción</th>
                        <th>Precio</th>
                        <th>Stock</th>
                        <th>Vendedor</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(function(i) { %>
                        <tr>
                            <td><%= i.nombre %></td>
                            <td><%= i.descripcion %></td>
                            <td><%= i.precio_original %></td>
                            <td><%= i.stock %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>    
    </main>
    
</body>
</html>